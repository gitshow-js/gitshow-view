export type Coordinates = {
    service: string;
    username: string;
    repo: string;
    branch: string;
    path: string
}

export type MessageCode = {
    code?: string;
    message?: string;
}
